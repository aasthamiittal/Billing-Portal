import CatalogManager from "../components/CatalogManager";

const Discounts = () => (
  <CatalogManager title="Discounts" kind="discounts" showValue valueLabel="Discount (%)" />
);

export default Discounts;

